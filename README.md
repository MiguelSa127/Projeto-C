Copie o config.example.h e renomeie para config.h e preencha com suas credenciais do banco de dados

Para por o icone é necessário rodar, antes de compilar:
  {caminho do gcc}\gcc\mingw64\bin\windres.exe resource.rc -o resource.o
